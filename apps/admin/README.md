# Arcana Admin

Scriptwriter admin panel for Arcana - Next.js (App Router) app talking to `apps/api`.

## Development

```bash
# from the repo root
pnpm --filter @arcana/admin dev
```

Needs `apps/api` running (see the backend's own README/CLAUDE.md) and a `.env.local` copied
from `.env.local.example`.
